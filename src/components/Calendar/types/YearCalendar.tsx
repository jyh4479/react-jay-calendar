import React from 'react';
import {CalendarProps} from "../Calendar";

const YearCalendar = (props: CalendarProps) => {
    const {type} = props;

    return (
        <div>Year</div>
    )
};

export default YearCalendar;